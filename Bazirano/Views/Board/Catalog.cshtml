@{
    ViewBag.Title = "Ploča";
    Layout = "_BoardLayout";
}

@model List<BoardThread>
    <div class="card-deck">
        @foreach (var thread in Model)
        {
            <partial name="BoardThreadPost" model="thread" />
        }
    </div>

<a class="btn btn-secondary p-2" href="#"> Na vrh </a>

<div class="p-3"></div>



